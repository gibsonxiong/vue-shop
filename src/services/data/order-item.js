class OrderItem {
  constructor(id, orderId, itemId,itemName, itemPropValues, price, quantity) {
    Object.assign(this,{
      id,
      orderId,
      itemId,
      itemName,
      itemPropValues,
      price,
      quantity
    })
  }
}

export default [

];